const a = 5
const b = 10

const add = function(x, y){
    return x + y;
}

const addarrow = (a, b) => {
    return a + b;
}

const subtract = (a, b) => {
    const result = a - b;
    return result;
}

console.log(`Addition: ${result}`)
console.log(`Addition : ${result}`)
console.log(`Addition: ${result}`)